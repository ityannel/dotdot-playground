[
    {"name": "alice", "age": 14},
    {"name": "bob", "age": 18}
] >> Map(user):
    user >> Update:
        ::is_adult >> check:
            is user::age >= 15: true.
            else: Drop.
        ..
    ..
.. >> Display.
