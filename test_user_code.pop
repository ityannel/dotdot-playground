[85, 92, 78, 95, 88, 64, 100] >> scores.

scores >> Max >> max.
scores >> Sum >> @ / (scores >> Length) >> avg.

"===================================================" >> Display.
"試験結果分析レポート" >> Display.
"===================================================" >> Display.
"最高点: {max}点" >> Display.
"平均点: {avg}点" >> Display.
"===================================================" >> Display.
